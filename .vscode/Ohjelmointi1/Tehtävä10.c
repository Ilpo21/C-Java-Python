#include <stdio.h>
int main(void)
{
    int i;
    int j;
    int k;
    int l;
    int N[10];

    for (j = 1; j < 11; j++) // ykköset
    {
    }
    while (i = 100)
    {
        i < 1000;
        i = i + 100;
    }
    // sadat
    while (k = 10)
    {
        k < 100;
        k = k + 10;
    }
    // kymmenet

    while (l = 1000)
    {
        l < 10000;
        l = l + 1000;
    }
    // tuhannet

    printf("%d %d %d %d", j, k, i, l);
    //---------------------------------------------------

    printf("\n%-7s%-7s%-7s%-7s", "N", "10N", "100N", "1000N");

    for (n = 0; n < 11; n = n + 1)
    {
        printf("%d \n %d\t %d\t %d\t", n, n10, n100, n1000);
    }

    return 0;

    n, n10, n100, n * 1000

                      for (n = 0; n < 11; n = n + 1)
    {
        printf("%d \n %d\t %d\t %d\t", n, n10, n100, n1000);
    }
}

return 0;
