#include <stdio.h>
#include <string.h>

void lukeluku(void);
void bufferi(void);

int main(void)
{
    char tilinumero[256];
    char pinnumero[256];
    char tarkistus[256];
    int tulos = 0;
    FILE *tFile;
}